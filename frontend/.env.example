# 복사: cp .env.example .env.local  (로컬 백엔드만 쓸 때)
#
# 미설정 시 기본값: https://llmz-team05.azurewebsites.net
# (.env.development / .env.production 에도 동일 URL이 설정되어 있음)

# Azure App Service (팀 공용 BE, 기본)
# VITE_API_BASE_URL=https://llmz-team05.azurewebsites.net

# 로컬 FastAPI만 사용할 때 (.env.local 권장, git 제외)
# VITE_API_BASE_URL=http://127.0.0.1:8000
